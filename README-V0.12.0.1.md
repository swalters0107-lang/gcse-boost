# LevelUp10 V0.12.0.1

Platform connection repair.

- Replaces RLS-sensitive upsert-return startup with explicit read/create/update operations.
- Cloud Account can no longer remain indefinitely on Checking connection if platform setup fails.
- Displays the actual platform error with a retry button while preserving existing GCSE progress.
- Keeps V0.11B.5.3 learning, Dragon, mission-resume and profile layout behaviour.
